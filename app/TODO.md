# TODO: Внедрение системы судейства IWUF (A-B)

## 1. База данных (Миграции)
- [ ] **Таблица `competitions`**:
    - Добавить поле `scoring_system` (enum: `'standard'`, `'iwuf_ab'`).
    - *Зачем:* Определяет логику интерфейсов и формул для всего турнира.
- [ ] **Таблица `competition_user` (Связь судья-турнир)**:
    - Добавить поле `role` (enum: `'referee'` (рефери/старший), `'judge_a'` (техника), `'judge_b'` (впечатление)).
    - *Зачем:* Разграничение прав и интерфейсов на пульте.
- [ ] **Таблица `registrations` (Выступления)**:
    - Добавить поля: 
        - `score_a` (decimal, итоговый балл А).
        - `score_b` (decimal, итоговый балл B).
        - `penalty` (decimal, штраф Старшего судьи).
    - *Зачем:* Хранить детализацию оценки для протоколов и истории, а не пересчитывать каждый раз.

## 2. Админ-панель (Filament)
- [ ] **Ресурс Соревнований (`CompetitionResource`)**:
    - В форму создания добавить Select: "Система судейства" (`Standard` / `IWUF A-B`).
- [ ] **Ресурс Судей (`JudgeResource` / RelationManager)**:
    - При добавлении судьи в бригаду добавить Select "Роль" (Судья А, Судья В, Старший).
    - В таблице судей вывести бейджи с ролями (А — Красный, В — Синий).

## 3. Пульт Линейного судьи (Frontend)
- [ ] **Логика маршрутизации**:
    - При входе проверять `scoring_system` турнира и `role` судьи.
- [ ] **Интерфейс Судьи А (Качество)**:
    - Отображать базу: **5.00**.
    - Кнопки сбавок (Checkbox или Buttons): `[-0.10]`, `[-0.20]`, `[-0.30]` (ошибки кода).
    - Логика: `5.00 - сумма сбавок`.
- [ ] **Интерфейс Судьи B (Впечатление)**:
    - Слайдер или поле ввода (диапазон 0.00 - 5.00/3.00).

## 4. Пульт Старшего судьи (Refree Console)
- [ ] **Мониторинг**:
    - Видеть оценки всех судей в реальном времени (отдельно столбец А, отдельно В).
- [ ] **Управление штрафами**:
    - Поле ввода `penalty` (Штраф Гл. судьи).
    - *Причины:* Время, выход за ковер, несоответствие костюма.
- [ ] **Финализация**:
    - Кнопка "Подтвердить оценку", которая запускает финальный расчет.

## 5. Математика (Backend / ScoreService)
- [ ] **Метод расчета (`calculateFinalScore`)**:
    - Добавить ветвление `if (system === 'iwuf_ab')`:
        1. Собрать оценки `judge_a` -> Найти среднее (или сумму сбавок) -> Сохранить в `score_a`.
        2. Собрать оценки `judge_b` -> Найти среднее -> Сохранить в `score_b`.
        3. Взять `penalty` из записи.
        4. Формула: `Total = Score_A + Score_B - Penalty`.

## 6. Визуализация и Экспорт
- [ ] **Электронное табло (Live Screen)**:
    - Если система A-B: показывать раскладку `A: 4.80 | B: 3.55 | Pen: 0.10 | Total: 8.25`.
- [ ] **PDF Дипломы и Протоколы**:
    - В итоговом протоколе разбить колонку оценки на три под-колонки (А, B, Штраф) или выводить строкой, как на табло.
